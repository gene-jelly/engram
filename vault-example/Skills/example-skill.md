# Example Skill

Use when working with [specific domain]. This skill encodes patterns and procedures that Claude should follow.

## When to Use

- When the user asks about [topic]
- When working with [specific tool or API]
- When [trigger condition]

## Key Patterns

1. Always check [thing] before [action]
2. The API uses [format] — common gotcha is [gotcha]
3. Prefer [approach A] over [approach B] because [reason]

## Quick Reference

```bash
# Common command
some-command --flag value

# Another useful pattern
other-command | filter | format
```

## Known Issues

- [Tool] silently truncates results beyond [limit] — always paginate
- [API] returns [unexpected value] when [edge case] — handle it explicitly
